MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=lty20023110.
MYSQL_DB=knowledge_graph  # 需提前创建数据库

# 数据库切换配置
# DB_TYPE 可选值:
#   'auto'   - 自动模式：优先使用 MySQL，MySQL 失败则自动切换到 SQLite（推荐）
#   'mysql'  - 强制使用 mysql
#   'sqlite' - 强制使用 sqlite
DB_TYPE=sqlite